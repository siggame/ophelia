sudo: required

services:
  - docker

language: nodejs

env:
  - POSTGRES_PORT=5433

before_script:
  - npm run setup
  - npm run build
  - docker run --name ophelia-db --publish 5433:5432 --detach siggame/colisee-db

script:
  - npm run quick-test

after_script:
  - docker stop ophelia-db
